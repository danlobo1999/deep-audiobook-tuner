# To Run: conda env create -f environment.yml
name: deepspeech
channels:
  - defaults
dependencies:
  - jupyter
  - ipyparallel
  - pip
  - python=3.6
  - pip:
      - deepspeech==0.6.0
      - wave==0.0.2
